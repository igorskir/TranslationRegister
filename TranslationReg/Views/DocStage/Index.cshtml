@model IEnumerable<TranslationRegistryModel.DocStage>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Document.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DocFile.Path)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Document.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DocFile.Path)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
