@model List<TranslationRegistryModel.User_Stage>
<div>
    <div class="titles">
        <div class="col-md-2">Пользователь</div>
        <div class="col-md-2">Комментарий</div>
        <div class="col-md-1">Количество</div>
        <div class="col-md-2">Назначено</div>
        <div class="col-md-2">Загружено</div>
        <div class="col-md-3">Команды</div>
    </div>

    @foreach (var userStage in Model)
    {
        @Html.Partial("../User_Stage/Item", userStage)
    }
</div>
